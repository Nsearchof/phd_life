# State log

Append-only, dated log of what advanced and what's queued next. Newest entries at the bottom. One entry per substantive working session.

## 2026-05-28 — repo scaffolded

**Advanced.** Initialized `phd_life` from the structure inspired by PyPhysics, customized for a theoretical + experimental physics PhD:
- Obsidian vault config + wikilink conventions.
- `Literature/` database — Bibliography (YAML cite-cards → BibTeX) + Papers + Reading_Notes.
- `Research/` split into Theory and Experiments (lab-notebook template).
- `Planning/` — `schedule.yaml` → `build_gantt.py` → `GANTT.md`, plus milestones + deliverables docs.
- `.dev/` tasking (this log + brief + task 1).
- uv project (`pyproject.toml`, sole dep `pyyaml`).

**Next.** Choose the first research thread and write it into [brief.md](brief.md); populate `Planning/schedule.yaml` with real program dates; add the first real Bibliography cite-cards.

**Status:** READY.
